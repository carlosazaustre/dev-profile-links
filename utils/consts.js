// Colors
const primaryColor = 'var(--nextui-colors-primary)'

// Sizes
const mw = '360px'
const videoWidth = '356px'
const videoHeight = '200px'

// Backgrounds
const bg = '#222'
const altBg = '#111'

// Texts
const primaryText = primaryColor
const defaultText = '#fff'
const altText = '#9e9e9e'

export const theme = {
  primaryColor,

  mw,
  videoWidth,
  videoHeight,

  bg,
  altBg,

  primaryText,
  defaultText,
  altText
}
